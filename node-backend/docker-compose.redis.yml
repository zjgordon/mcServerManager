version: '3.8'

services:
  redis:
    image: redis:7-alpine
    container_name: mcserver-redis-dev
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
      - ./redis.conf:/usr/local/etc/redis/redis.conf:ro
    command: redis-server /usr/local/etc/redis/redis.conf
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 30s
    networks:
      - mcserver-network

  redis-commander:
    image: rediscommander/redis-commander:latest
    container_name: mcserver-redis-commander
    ports:
      - "8081:8081"
    environment:
      - REDIS_HOSTS=local:redis:6379
      - HTTP_USER=admin
      - HTTP_PASSWORD=admin123
    depends_on:
      - redis
    restart: unless-stopped
    networks:
      - mcserver-network

volumes:
  redis_data:
    driver: local

networks:
  mcserver-network:
    driver: bridge

